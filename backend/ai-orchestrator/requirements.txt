# ====================================
# AI Orchestrator - Pilar 3: MCP Chatbot
# ====================================

# --- Core Framework ---
fastapi==0.115.2
uvicorn[standard]==0.31.1
python-dotenv==1.0.1
pydantic==2.8.2
pydantic-settings==2.5.2

# --- LLM Providers ---
openai==1.54.3                    # OpenAI API
google-generativeai==0.8.3        # Gemini API
anthropic==0.39.0                 # Claude (opcional)

# --- HTTP Client ---
httpx==0.27.2                     # Cliente HTTP async para Rails API

# --- Multimodal Processing ---
# Imágenes
pillow==11.0.0                    # Procesamiento de imágenes
pytesseract==0.3.13               # OCR local (requiere Tesseract instalado)
# google-cloud-vision==3.7.4      # OCR en la nube (opcional, comentado por costos)

# PDF
PyPDF2==3.0.1                     # Extracción de texto de PDFs
pdfplumber==0.11.4                # Análisis avanzado de PDFs (tablas, layouts)

# Audio (BONUS)
# openai-whisper==20231117        # Transcripción de audio (opcional)
# speechrecognition==3.11.0       # Alternativa más ligera

# --- WebSocket Client ---
python-socketio[client]==5.11.4   # Cliente para conectar con NestJS WebSocket
websockets==13.1                  # WebSocket async

# --- Logging y Utilidades ---
loguru==0.7.2                     # Logging avanzado
python-multipart==0.0.12          # Para upload de archivos

# --- Validation & Security ---
validators==0.34.0                # Validación de URLs, emails, etc.

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==6.0.0
# httpx-mock==0.8.0  # Versión no disponible, comentado

# --- Code Quality ---
black==24.10.0
flake8==7.1.1
mypy==1.13.0
